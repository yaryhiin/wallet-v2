export type AuthErrors = {
  confirmPassword: boolean;
  password: boolean;
  email: boolean;
};

export type AccountErrors = {
  name: boolean;
  balance: boolean;
  currency: boolean;
  icon: boolean;
};
